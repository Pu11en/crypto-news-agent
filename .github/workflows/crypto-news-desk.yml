name: crypto-news-desk

on:
  push:
    paths:
      - 'skills/crypto-news-desk/**'
      - 'dist/crypto-news-desk.skill'
      - '.github/workflows/crypto-news-desk.yml'
  pull_request:
    paths:
      - 'skills/crypto-news-desk/**'
      - 'dist/crypto-news-desk.skill'
      - '.github/workflows/crypto-news-desk.yml'

permissions:
  contents: read

jobs:
  test:
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            python: '3.10'
          - os: ubuntu-latest
            python: '3.12'
          - os: windows-latest
            python: '3.12'
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python }}
      - name: Run isolated skill tests
        shell: bash
        env:
          CRYPTO_NEWS_DESK_HOME: ${{ runner.temp }}/crypto-news-desk
        run: python skills/crypto-news-desk/scripts/run.py test
      - name: Verify pre-auth doctor
        shell: bash
        env:
          CRYPTO_NEWS_DESK_HOME: ${{ runner.temp }}/crypto-signal-doctor
        run: |
          python skills/crypto-news-desk/scripts/run.py init --acknowledge-x-terms-risk
          python skills/crypto-news-desk/scripts/run.py doctor

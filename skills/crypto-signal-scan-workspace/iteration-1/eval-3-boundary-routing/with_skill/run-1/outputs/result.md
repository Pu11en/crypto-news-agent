I can’t complete that request with `crypto-signal-scan`. This skill is collection-only: it may gather raw, attributable public X posts, but it must not rank stories, summarize claims, or write scripts. I also did not access credentials or run a live X scan.

To proceed safely, first use this skill to produce a raw JSONL export from a user-owned X session, then pass that export to a separate analysis/writing workflow to select three stories and draft the TikTok script.
